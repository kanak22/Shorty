<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    
    <title>Shorty - URL shortner</title>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>SHORTY</h1>
            <p>Your URL Shortener</p>
        </div>
       
        <div class="wrapper">
            <form action="/shortURLs" method="post" class="my-4 form-inline">
                <label for="fullURL" class="visually-hidden">URL</label>
                <input type="url" name="fullURL" id="fullURL" placeholder="Enter your URL" required class="form-control mr-2 col" >
                <br/>
                <div class="text-center d-grid gap-2">
                    <button type="submit" class="btn btn-warning">Shorten It</button>
                </div>
                
            </form>
        </div>
    
     <div class="cover">
        <table class="table table-stripped table-responsive table-info">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% shortUrls.forEach(shortUrl => { %>
                    <tr>
                    <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                    <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                    <td><%= shortUrl.clicks %></td>
                </tr>
                <%});%>
                
            </tbody>
        </table>
     </div>
   
    </div>
    
</body>
</html>